package com.coderank.submission.enums;

/**
 * RUN  – ad-hoc execution with custom stdin (no judging).
 * SUBMIT – full judge run against all test cases for a problem.
 */
public enum SubmissionType {
    RUN,
    SUBMIT
}
